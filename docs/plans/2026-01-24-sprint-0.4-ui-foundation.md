# Sprint 0.4: UI Foundation Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Configure Tailwind v4 with design tokens, install shadcn/ui primitives, and load custom fonts so both apps can use styled UI components.

**Architecture:** Tailwind CSS v4 is configured in a shared preset within packages/ui, then consumed by both apps. shadcn/ui components are installed into packages/ui and exported for use across the monorepo. Fonts are loaded via next/font in each app's layout.

**Tech Stack:** Tailwind CSS v4, PostCSS, shadcn/ui, class-variance-authority (cva), clsx, tailwind-merge, next/font (Inter, Plus Jakarta Sans)

---

## Prerequisites

- Sprint 0.1-0.3 completed (monorepo structure, database, auth)
- Node.js 20+, pnpm 9.15+
- Access to `/mnt/c/VelikiBukovec_web`

## Acceptance Criteria (from ROADMAP.md)

- [ ] Tailwind v4 configured with design tokens from DESIGN-SYSTEM.md
- [ ] shadcn/ui initialized (Button, Input, Card, Label)
- [ ] Inter + Plus Jakarta Sans fonts loaded
- [ ] Color palette matches design tokens
- [ ] packages/ui exports primitives
- [ ] Both apps can import from @repo/ui

**Gate:** Button with all variants renders correctly in both apps

---

## Task 1: Install Tailwind CSS v4 and Dependencies

**Files:**
- Modify: `packages/ui/package.json`
- Modify: `apps/web/package.json`
- Modify: `apps/admin/package.json`

**Step 1: Install dependencies in packages/ui**

```bash
cd /mnt/c/VelikiBukovec_web
pnpm --filter @repo/ui add -D tailwindcss@^4.0.0 @tailwindcss/postcss postcss autoprefixer
pnpm --filter @repo/ui add class-variance-authority clsx tailwind-merge
```

**Step 2: Install Tailwind in web app**

```bash
pnpm --filter @repo/web add -D tailwindcss@^4.0.0 @tailwindcss/postcss postcss autoprefixer
```

**Step 3: Install Tailwind in admin app**

```bash
pnpm --filter @repo/admin add -D tailwindcss@^4.0.0 @tailwindcss/postcss postcss autoprefixer
```

**Step 4: Verify installation**

Run: `pnpm install`
Expected: No errors, lock file updated

**Step 5: Commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
chore: install Tailwind CSS v4 and dependencies

- Added tailwindcss, postcss, autoprefixer to ui, web, admin packages
- Added class-variance-authority, clsx, tailwind-merge to ui package

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: Create Tailwind Configuration with Design Tokens

**Files:**
- Create: `packages/ui/tailwind.config.ts`
- Create: `packages/ui/postcss.config.js`
- Create: `packages/ui/src/lib/utils.ts`

**Step 1: Create the cn utility function**

Create `packages/ui/src/lib/utils.ts`:

```typescript
import { type ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

**Step 2: Create Tailwind config with design tokens from DESIGN-SYSTEM.md**

Create `packages/ui/tailwind.config.ts`:

```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './src/**/*.{js,ts,jsx,tsx}',
    // Include consuming apps
    '../../apps/*/app/**/*.{js,ts,jsx,tsx}',
    '../../apps/*/components/**/*.{js,ts,jsx,tsx}',
  ],
  theme: {
    extend: {
      colors: {
        // Brand - Based on Veliki Bukovec crest (greens + gold)
        primary: {
          50: '#f0fdf4',
          100: '#dcfce7',
          200: '#bbf7d0',
          300: '#86efac',
          400: '#4ade80',
          500: '#22c55e', // Main brand green
          600: '#16a34a',
          700: '#15803d',
          800: '#166534',
          900: '#14532d',
        },
        accent: {
          400: '#facc15',
          500: '#eab308', // Gold/yellow from crest
          600: '#ca8a04',
        },
        // Semantic
        success: '#22c55e',
        warning: '#f59e0b',
        error: '#ef4444',
        info: '#3b82f6',
        // Neutrals (slate for professional government feel)
        neutral: {
          50: '#f8fafc',
          100: '#f1f5f9',
          200: '#e2e8f0',
          300: '#cbd5e1',
          400: '#94a3b8',
          500: '#64748b',
          600: '#475569',
          700: '#334155',
          800: '#1e293b',
          900: '#0f172a',
        },
      },
      fontFamily: {
        sans: ['var(--font-inter)', 'system-ui', 'sans-serif'],
        display: ['var(--font-plus-jakarta)', 'sans-serif'],
      },
      borderRadius: {
        sm: '0.25rem',
        DEFAULT: '0.5rem',
        lg: '0.75rem',
        xl: '1rem',
      },
      boxShadow: {
        sm: '0 1px 2px 0 rgb(0 0 0 / 0.05)',
        DEFAULT: '0 1px 3px 0 rgb(0 0 0 / 0.1)',
        md: '0 4px 6px -1px rgb(0 0 0 / 0.1)',
        lg: '0 10px 15px -3px rgb(0 0 0 / 0.1)',
      },
      transitionDuration: {
        instant: '50ms',
        fast: '150ms',
        normal: '250ms',
        slow: '350ms',
        slower: '500ms',
      },
    },
  },
  plugins: [],
};

export default config;
```

**Step 3: Create PostCSS config**

Create `packages/ui/postcss.config.js`:

```javascript
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
};
```

**Step 4: Export utils from package**

Update `packages/ui/src/index.ts` to add:

```typescript
export * from './primitives';
export * from './lib/utils';
```

**Step 5: Commit**

```bash
git add packages/ui/tailwind.config.ts packages/ui/postcss.config.js packages/ui/src/lib/utils.ts packages/ui/src/index.ts
git commit -m "$(cat <<'EOF'
feat(ui): add Tailwind config with design tokens

- Created tailwind.config.ts with colors, fonts, spacing from DESIGN-SYSTEM.md
- Created postcss.config.js for Tailwind processing
- Added cn() utility for className merging

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Configure Apps to Use Tailwind

**Files:**
- Create: `apps/web/postcss.config.js`
- Create: `apps/web/tailwind.config.ts`
- Modify: `apps/web/app/globals.css`
- Create: `apps/admin/postcss.config.js`
- Create: `apps/admin/tailwind.config.ts`
- Modify: `apps/admin/app/globals.css`

**Step 1: Create web app PostCSS config**

Create `apps/web/postcss.config.js`:

```javascript
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
};
```

**Step 2: Create web app Tailwind config that extends UI config**

Create `apps/web/tailwind.config.ts`:

```typescript
import type { Config } from 'tailwindcss';
import baseConfig from '@repo/ui/tailwind.config';

const config: Config = {
  presets: [baseConfig],
  content: [
    './app/**/*.{js,ts,jsx,tsx}',
    './components/**/*.{js,ts,jsx,tsx}',
    '../../packages/ui/src/**/*.{js,ts,jsx,tsx}',
  ],
};

export default config;
```

**Step 3: Update web app globals.css**

Replace `apps/web/app/globals.css`:

```css
@import 'tailwindcss';

@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
  }

  * {
    box-sizing: border-box;
  }

  body {
    @apply bg-white text-neutral-900 antialiased;
    font-feature-settings: 'rlig' 1, 'calt' 1;
  }
}
```

**Step 4: Create admin app PostCSS config**

Create `apps/admin/postcss.config.js`:

```javascript
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
};
```

**Step 5: Create admin app Tailwind config**

Create `apps/admin/tailwind.config.ts`:

```typescript
import type { Config } from 'tailwindcss';
import baseConfig from '@repo/ui/tailwind.config';

const config: Config = {
  presets: [baseConfig],
  content: [
    './app/**/*.{js,ts,jsx,tsx}',
    './components/**/*.{js,ts,jsx,tsx}',
    '../../packages/ui/src/**/*.{js,ts,jsx,tsx}',
  ],
};

export default config;
```

**Step 6: Update admin app globals.css**

Replace `apps/admin/app/globals.css`:

```css
@import 'tailwindcss';

@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
  }

  * {
    box-sizing: border-box;
  }

  body {
    @apply bg-neutral-50 text-neutral-900 antialiased;
    font-feature-settings: 'rlig' 1, 'calt' 1;
  }
}
```

**Step 7: Update packages/ui package.json to export tailwind config**

Add to `packages/ui/package.json` exports:

```json
{
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./tailwind.config": {
      "types": "./tailwind.config.ts",
      "default": "./tailwind.config.ts"
    }
  }
}
```

**Step 8: Verify Tailwind works**

Run: `cd /mnt/c/VelikiBukovec_web && pnpm build`
Expected: Build succeeds without CSS errors

**Step 9: Commit**

```bash
git add apps/web/postcss.config.js apps/web/tailwind.config.ts apps/web/app/globals.css \
        apps/admin/postcss.config.js apps/admin/tailwind.config.ts apps/admin/app/globals.css \
        packages/ui/package.json
git commit -m "$(cat <<'EOF'
feat: configure Tailwind CSS in web and admin apps

- Apps extend base config from @repo/ui
- Updated globals.css with Tailwind directives
- Exported tailwind.config from ui package

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Setup Font Loading

**Files:**
- Modify: `apps/web/app/layout.tsx`
- Modify: `apps/admin/app/layout.tsx`

**Step 1: Update web app layout with fonts**

Modify `apps/web/app/layout.tsx`:

```typescript
import type { Metadata } from 'next';
import { Inter, Plus_Jakarta_Sans } from 'next/font/google';

import './globals.css';

const inter = Inter({
  subsets: ['latin', 'latin-ext'],
  variable: '--font-inter',
  display: 'swap',
});

const plusJakarta = Plus_Jakarta_Sans({
  subsets: ['latin', 'latin-ext'],
  variable: '--font-plus-jakarta',
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'Općina Veliki Bukovec',
  description: 'Službena web stranica Općine Veliki Bukovec',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="hr" className={`${inter.variable} ${plusJakarta.variable}`}>
      <body className="font-sans">{children}</body>
    </html>
  );
}
```

**Step 2: Update admin app layout with fonts**

Modify `apps/admin/app/layout.tsx`:

```typescript
import type { Metadata } from 'next';
import { Inter, Plus_Jakarta_Sans } from 'next/font/google';

import { SessionProvider } from '@/components/providers/session-provider';

import './globals.css';

const inter = Inter({
  subsets: ['latin', 'latin-ext'],
  variable: '--font-inter',
  display: 'swap',
});

const plusJakarta = Plus_Jakarta_Sans({
  subsets: ['latin', 'latin-ext'],
  variable: '--font-plus-jakarta',
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'Admin | Općina Veliki Bukovec',
  description: 'Administracija web stranice Općine Veliki Bukovec',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="hr" className={`${inter.variable} ${plusJakarta.variable}`}>
      <body className="font-sans">
        <SessionProvider>{children}</SessionProvider>
      </body>
    </html>
  );
}
```

**Step 3: Verify fonts load**

Run: `cd /mnt/c/VelikiBukovec_web && pnpm build`
Expected: Build succeeds, fonts included in output

**Step 4: Commit**

```bash
git add apps/web/app/layout.tsx apps/admin/app/layout.tsx
git commit -m "$(cat <<'EOF'
feat: add Inter and Plus Jakarta Sans font loading

- Configured next/font for Inter (body) and Plus Jakarta Sans (headings)
- Added CSS variables for Tailwind font-family integration
- Both apps now use custom fonts

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Create Button Component with CVA

**Files:**
- Modify: `packages/ui/src/primitives/button.tsx`
- Modify: `packages/ui/src/primitives/index.ts`

**Step 1: Rewrite Button component using CVA and Tailwind**

Replace `packages/ui/src/primitives/button.tsx`:

```typescript
import { forwardRef, type ButtonHTMLAttributes } from 'react';
import { cva, type VariantProps } from 'class-variance-authority';

import { cn } from '../lib/utils';

const buttonVariants = cva(
  // Base styles
  'inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md font-medium transition-colors duration-fast focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500 focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50',
  {
    variants: {
      variant: {
        primary: 'bg-primary-600 text-white hover:bg-primary-700 active:bg-primary-800',
        secondary: 'bg-neutral-100 text-neutral-900 hover:bg-neutral-200 active:bg-neutral-300',
        outline: 'border border-neutral-300 bg-white text-neutral-900 hover:bg-neutral-50 active:bg-neutral-100',
        ghost: 'text-neutral-700 hover:bg-neutral-100 active:bg-neutral-200',
        danger: 'bg-error text-white hover:bg-red-600 active:bg-red-700',
        link: 'text-primary-600 underline-offset-4 hover:underline',
      },
      size: {
        sm: 'h-8 px-3 text-sm',
        md: 'h-10 px-4 text-sm',
        lg: 'h-12 px-6 text-base',
        icon: 'h-10 w-10',
      },
    },
    defaultVariants: {
      variant: 'primary',
      size: 'md',
    },
  }
);

export interface ButtonProps
  extends ButtonHTMLAttributes<HTMLButtonElement>,
    VariantProps<typeof buttonVariants> {
  asChild?: boolean;
}

const Button = forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant, size, ...props }, ref) => {
    return (
      <button
        className={cn(buttonVariants({ variant, size, className }))}
        ref={ref}
        {...props}
      />
    );
  }
);
Button.displayName = 'Button';

export { Button, buttonVariants };
```

**Step 2: Update primitives index**

Replace `packages/ui/src/primitives/index.ts`:

```typescript
export { Button, buttonVariants, type ButtonProps } from './button';
```

**Step 3: Verify type-check passes**

Run: `cd /mnt/c/VelikiBukovec_web && pnpm type-check`
Expected: All packages pass

**Step 4: Commit**

```bash
git add packages/ui/src/primitives/button.tsx packages/ui/src/primitives/index.ts
git commit -m "$(cat <<'EOF'
feat(ui): rewrite Button with CVA and Tailwind

- Added all variants: primary, secondary, outline, ghost, danger, link
- Added all sizes: sm, md, lg, icon
- Uses cn() for className merging
- Forwards ref properly

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: Create Input Component

**Files:**
- Create: `packages/ui/src/primitives/input.tsx`
- Modify: `packages/ui/src/primitives/index.ts`

**Step 1: Create Input component**

Create `packages/ui/src/primitives/input.tsx`:

```typescript
import { forwardRef, type InputHTMLAttributes } from 'react';

import { cn } from '../lib/utils';

export interface InputProps extends InputHTMLAttributes<HTMLInputElement> {
  error?: boolean;
}

const Input = forwardRef<HTMLInputElement, InputProps>(
  ({ className, type, error, ...props }, ref) => {
    return (
      <input
        type={type}
        className={cn(
          'flex h-10 w-full rounded-md border bg-white px-3 py-2 text-sm transition-colors duration-fast',
          'placeholder:text-neutral-400',
          'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500 focus-visible:ring-offset-2',
          'disabled:cursor-not-allowed disabled:opacity-50',
          error
            ? 'border-error focus-visible:ring-error'
            : 'border-neutral-300 hover:border-neutral-400',
          className
        )}
        ref={ref}
        {...props}
      />
    );
  }
);
Input.displayName = 'Input';

export { Input };
```

**Step 2: Export Input from primitives**

Update `packages/ui/src/primitives/index.ts`:

```typescript
export { Button, buttonVariants, type ButtonProps } from './button';
export { Input, type InputProps } from './input';
```

**Step 3: Verify type-check**

Run: `cd /mnt/c/VelikiBukovec_web && pnpm type-check`
Expected: Pass

**Step 4: Commit**

```bash
git add packages/ui/src/primitives/input.tsx packages/ui/src/primitives/index.ts
git commit -m "$(cat <<'EOF'
feat(ui): add Input component

- Styled input with focus ring and error state
- Forwards ref properly

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: Create Label Component

**Files:**
- Create: `packages/ui/src/primitives/label.tsx`
- Modify: `packages/ui/src/primitives/index.ts`

**Step 1: Create Label component**

Create `packages/ui/src/primitives/label.tsx`:

```typescript
import { forwardRef, type LabelHTMLAttributes } from 'react';
import { cva, type VariantProps } from 'class-variance-authority';

import { cn } from '../lib/utils';

const labelVariants = cva(
  'text-sm font-medium leading-none peer-disabled:cursor-not-allowed peer-disabled:opacity-70'
);

export interface LabelProps
  extends LabelHTMLAttributes<HTMLLabelElement>,
    VariantProps<typeof labelVariants> {
  required?: boolean;
}

const Label = forwardRef<HTMLLabelElement, LabelProps>(
  ({ className, required, children, ...props }, ref) => {
    return (
      <label
        ref={ref}
        className={cn(labelVariants(), className)}
        {...props}
      >
        {children}
        {required && <span className="ml-1 text-error">*</span>}
      </label>
    );
  }
);
Label.displayName = 'Label';

export { Label, labelVariants };
```

**Step 2: Export Label from primitives**

Update `packages/ui/src/primitives/index.ts`:

```typescript
export { Button, buttonVariants, type ButtonProps } from './button';
export { Input, type InputProps } from './input';
export { Label, labelVariants, type LabelProps } from './label';
```

**Step 3: Commit**

```bash
git add packages/ui/src/primitives/label.tsx packages/ui/src/primitives/index.ts
git commit -m "$(cat <<'EOF'
feat(ui): add Label component

- Styled label with required indicator support

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: Create Card Component

**Files:**
- Create: `packages/ui/src/primitives/card.tsx`
- Modify: `packages/ui/src/primitives/index.ts`

**Step 1: Create Card component with subcomponents**

Create `packages/ui/src/primitives/card.tsx`:

```typescript
import { forwardRef, type HTMLAttributes } from 'react';

import { cn } from '../lib/utils';

const Card = forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement>>(
  ({ className, ...props }, ref) => (
    <div
      ref={ref}
      className={cn(
        'rounded-lg border border-neutral-200 bg-white shadow-sm',
        className
      )}
      {...props}
    />
  )
);
Card.displayName = 'Card';

const CardHeader = forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement>>(
  ({ className, ...props }, ref) => (
    <div
      ref={ref}
      className={cn('flex flex-col space-y-1.5 p-6', className)}
      {...props}
    />
  )
);
CardHeader.displayName = 'CardHeader';

const CardTitle = forwardRef<HTMLHeadingElement, HTMLAttributes<HTMLHeadingElement>>(
  ({ className, ...props }, ref) => (
    <h3
      ref={ref}
      className={cn('font-display text-2xl font-semibold leading-none tracking-tight', className)}
      {...props}
    />
  )
);
CardTitle.displayName = 'CardTitle';

const CardDescription = forwardRef<HTMLParagraphElement, HTMLAttributes<HTMLParagraphElement>>(
  ({ className, ...props }, ref) => (
    <p
      ref={ref}
      className={cn('text-sm text-neutral-500', className)}
      {...props}
    />
  )
);
CardDescription.displayName = 'CardDescription';

const CardContent = forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement>>(
  ({ className, ...props }, ref) => (
    <div ref={ref} className={cn('p-6 pt-0', className)} {...props} />
  )
);
CardContent.displayName = 'CardContent';

const CardFooter = forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement>>(
  ({ className, ...props }, ref) => (
    <div
      ref={ref}
      className={cn('flex items-center p-6 pt-0', className)}
      {...props}
    />
  )
);
CardFooter.displayName = 'CardFooter';

export { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter };
```

**Step 2: Export Card from primitives**

Update `packages/ui/src/primitives/index.ts`:

```typescript
export { Button, buttonVariants, type ButtonProps } from './button';
export { Input, type InputProps } from './input';
export { Label, labelVariants, type LabelProps } from './label';
export {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
} from './card';
```

**Step 3: Commit**

```bash
git add packages/ui/src/primitives/card.tsx packages/ui/src/primitives/index.ts
git commit -m "$(cat <<'EOF'
feat(ui): add Card component with subcomponents

- Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
- Uses display font for titles

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: Update Web App to Use New Components

**Files:**
- Modify: `apps/web/app/page.tsx`

**Step 1: Update web homepage to showcase components**

Replace `apps/web/app/page.tsx`:

```typescript
import { APP_NAME } from '@repo/shared';
import {
  Button,
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
  Input,
  Label,
} from '@repo/ui';

export default function HomePage() {
  return (
    <main className="min-h-screen p-8">
      <div className="mx-auto max-w-4xl space-y-8">
        {/* Header */}
        <header className="text-center">
          <h1 className="font-display text-4xl font-bold text-primary-700">
            {APP_NAME}
          </h1>
          <p className="mt-2 text-neutral-600">
            Službena web stranica Općine Veliki Bukovec
          </p>
        </header>

        {/* Button Variants */}
        <Card>
          <CardHeader>
            <CardTitle>Button Variants</CardTitle>
            <CardDescription>All available button styles</CardDescription>
          </CardHeader>
          <CardContent className="flex flex-wrap gap-4">
            <Button variant="primary">Primary</Button>
            <Button variant="secondary">Secondary</Button>
            <Button variant="outline">Outline</Button>
            <Button variant="ghost">Ghost</Button>
            <Button variant="danger">Danger</Button>
            <Button variant="link">Link</Button>
          </CardContent>
        </Card>

        {/* Button Sizes */}
        <Card>
          <CardHeader>
            <CardTitle>Button Sizes</CardTitle>
            <CardDescription>Small, medium, and large sizes</CardDescription>
          </CardHeader>
          <CardContent className="flex flex-wrap items-center gap-4">
            <Button size="sm">Small</Button>
            <Button size="md">Medium</Button>
            <Button size="lg">Large</Button>
          </CardContent>
        </Card>

        {/* Form Elements */}
        <Card>
          <CardHeader>
            <CardTitle>Form Elements</CardTitle>
            <CardDescription>Input and label components</CardDescription>
          </CardHeader>
          <CardContent className="space-y-4">
            <div className="space-y-2">
              <Label htmlFor="email" required>
                Email
              </Label>
              <Input id="email" type="email" placeholder="ime@primjer.hr" />
            </div>
            <div className="space-y-2">
              <Label htmlFor="error-input">Input with error</Label>
              <Input id="error-input" error placeholder="Invalid input" />
            </div>
          </CardContent>
        </Card>

        {/* Typography Demo */}
        <Card>
          <CardHeader>
            <CardTitle>Typography</CardTitle>
            <CardDescription>Font families and sizes</CardDescription>
          </CardHeader>
          <CardContent className="space-y-4">
            <p className="font-display text-2xl font-bold">
              Plus Jakarta Sans (Display)
            </p>
            <p className="font-sans text-base">
              Inter (Body text) - Lorem ipsum dolor sit amet, consectetur
              adipiscing elit. Sed do eiusmod tempor incididunt ut labore.
            </p>
          </CardContent>
        </Card>

        {/* Color Palette */}
        <Card>
          <CardHeader>
            <CardTitle>Color Palette</CardTitle>
            <CardDescription>Brand and semantic colors</CardDescription>
          </CardHeader>
          <CardContent>
            <div className="grid grid-cols-5 gap-2">
              <div className="h-12 rounded bg-primary-500" title="Primary 500" />
              <div className="h-12 rounded bg-primary-600" title="Primary 600" />
              <div className="h-12 rounded bg-primary-700" title="Primary 700" />
              <div className="h-12 rounded bg-accent-500" title="Accent 500" />
              <div className="h-12 rounded bg-neutral-500" title="Neutral 500" />
            </div>
            <div className="mt-4 grid grid-cols-4 gap-2">
              <div className="h-8 rounded bg-success" title="Success" />
              <div className="h-8 rounded bg-warning" title="Warning" />
              <div className="h-8 rounded bg-error" title="Error" />
              <div className="h-8 rounded bg-info" title="Info" />
            </div>
          </CardContent>
        </Card>
      </div>
    </main>
  );
}
```

**Step 2: Verify build**

Run: `cd /mnt/c/VelikiBukovec_web && pnpm build`
Expected: Build succeeds

**Step 3: Commit**

```bash
git add apps/web/app/page.tsx
git commit -m "$(cat <<'EOF'
feat(web): update homepage to showcase UI components

- Demonstrates all button variants and sizes
- Shows form elements (Input, Label)
- Displays typography and color palette

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: Update Admin App to Use New Components

**Files:**
- Modify: `apps/admin/app/page.tsx`
- Modify: `apps/admin/app/login/page.tsx`

**Step 1: Update admin dashboard with Tailwind**

Replace `apps/admin/app/page.tsx`:

```typescript
'use client';

import { APP_NAME } from '@repo/shared';
import {
  Button,
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from '@repo/ui';
import { useRouter } from 'next/navigation';

import { ProtectedRoute } from '@/components/auth/protected-route';
import { useAuth } from '@/components/providers/session-provider';
import { signOut } from '@/lib/auth-client';

function DashboardContent() {
  const { user } = useAuth();
  const router = useRouter();

  const handleLogout = async () => {
    await signOut();
    router.push('/login');
  };

  return (
    <main className="min-h-screen bg-neutral-50 p-8">
      <div className="mx-auto max-w-4xl space-y-6">
        {/* Header */}
        <div className="flex items-center justify-between">
          <h1 className="font-display text-3xl font-bold text-neutral-900">
            {APP_NAME} - Admin
          </h1>
          <Button variant="secondary" onClick={() => void handleLogout()}>
            Odjava
          </Button>
        </div>

        {/* Welcome Card */}
        <Card>
          <CardHeader>
            <CardTitle>Dobrodošli!</CardTitle>
            <CardDescription>
              Prijavljeni ste u administracijsko sučelje
            </CardDescription>
          </CardHeader>
          <CardContent className="space-y-2">
            <p className="text-neutral-700">
              Email: <span className="font-medium">{user?.email}</span>
            </p>
            <p className="text-neutral-700">
              Uloga:{' '}
              <span className="inline-flex items-center rounded-full bg-primary-100 px-2.5 py-0.5 text-sm font-medium text-primary-800">
                {user?.role || 'staff'}
              </span>
            </p>
          </CardContent>
        </Card>

        {/* Info Card */}
        <Card>
          <CardHeader>
            <CardTitle>Sprint 0.4 - UI Foundation</CardTitle>
            <CardDescription>Tailwind CSS v4 + shadcn/ui components</CardDescription>
          </CardHeader>
          <CardContent>
            <p className="text-neutral-600">
              Administratorske funkcije bit će dodane u sljedećim sprintovima.
            </p>
            <div className="mt-4 flex gap-2">
              <Button variant="primary" size="sm">
                Primary
              </Button>
              <Button variant="secondary" size="sm">
                Secondary
              </Button>
              <Button variant="outline" size="sm">
                Outline
              </Button>
            </div>
          </CardContent>
        </Card>
      </div>
    </main>
  );
}

export default function AdminHomePage() {
  return (
    <ProtectedRoute>
      <DashboardContent />
    </ProtectedRoute>
  );
}
```

**Step 2: Update login page with Tailwind**

Replace `apps/admin/app/login/page.tsx`:

```typescript
'use client';

import { Button, Card, CardContent, CardHeader, CardTitle, Input, Label } from '@repo/ui';
import { useRouter } from 'next/navigation';
import { useState } from 'react';

import { signIn } from '@/lib/auth-client';

export default function LoginPage() {
  const router = useRouter();
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');
  const [isLoading, setIsLoading] = useState(false);

  const handleEmailLogin = async (e: React.FormEvent) => {
    e.preventDefault();
    setError('');
    setIsLoading(true);

    try {
      const result = await signIn.email({
        email,
        password,
      });

      if (result.error) {
        setError(result.error.message || 'Prijava nije uspjela');
      } else {
        router.push('/');
      }
    } catch {
      setError('Došlo je do greške. Pokušajte ponovo.');
    } finally {
      setIsLoading(false);
    }
  };

  const handleGoogleLogin = async () => {
    setError('');
    setIsLoading(true);

    try {
      await signIn.social({
        provider: 'google',
        callbackURL: '/',
      });
    } catch {
      setError('Google prijava nije uspjela');
      setIsLoading(false);
    }
  };

  return (
    <main className="flex min-h-screen items-center justify-center bg-neutral-50 p-4">
      <Card className="w-full max-w-md">
        <CardHeader className="text-center">
          <CardTitle>Prijava u administraciju</CardTitle>
        </CardHeader>
        <CardContent className="space-y-6">
          {error && (
            <div className="rounded-md bg-red-50 p-3 text-sm text-error">
              {error}
            </div>
          )}

          <form onSubmit={(e) => void handleEmailLogin(e)} className="space-y-4">
            <div className="space-y-2">
              <Label htmlFor="email">Email</Label>
              <Input
                id="email"
                type="email"
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                required
                placeholder="ime@primjer.hr"
              />
            </div>

            <div className="space-y-2">
              <Label htmlFor="password">Lozinka</Label>
              <Input
                id="password"
                type="password"
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                required
                placeholder="••••••••"
              />
            </div>

            <Button type="submit" className="w-full" disabled={isLoading}>
              {isLoading ? 'Prijava...' : 'Prijavi se'}
            </Button>
          </form>

          <div className="relative">
            <div className="absolute inset-0 flex items-center">
              <span className="w-full border-t border-neutral-300" />
            </div>
            <div className="relative flex justify-center text-sm">
              <span className="bg-white px-2 text-neutral-500">ili</span>
            </div>
          </div>

          <Button
            type="button"
            variant="outline"
            className="w-full"
            onClick={() => void handleGoogleLogin()}
            disabled={isLoading}
          >
            Prijavi se s Google računom
          </Button>
        </CardContent>
      </Card>
    </main>
  );
}
```

**Step 3: Verify build**

Run: `cd /mnt/c/VelikiBukovec_web && pnpm build`
Expected: Build succeeds

**Step 4: Commit**

```bash
git add apps/admin/app/page.tsx apps/admin/app/login/page.tsx
git commit -m "$(cat <<'EOF'
feat(admin): update pages to use Tailwind and UI components

- Dashboard uses Card, Button with proper styling
- Login page uses Card, Input, Label, Button
- Removed all inline styles

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: Final Verification and Documentation

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `ROADMAP.md`

**Step 1: Run all checks**

```bash
cd /mnt/c/VelikiBukovec_web
pnpm type-check
pnpm lint
pnpm build
```

Expected: All pass

**Step 2: Update CHANGELOG.md**

Add Sprint 0.4 entry to CHANGELOG.md after "Unreleased":

```markdown
## Sprint 0.4 - UI Foundation (Completed)
- Tailwind CSS v4 configured with design tokens from DESIGN-SYSTEM.md
- PostCSS configured in all packages
- Inter and Plus Jakarta Sans fonts loaded via next/font
- Button component with CVA variants (primary, secondary, outline, ghost, danger, link)
- Input component with error state
- Label component with required indicator
- Card component with subcomponents (Header, Title, Description, Content, Footer)
- cn() utility for className merging
- Both apps updated to use Tailwind styling
- Gate: Button with all variants renders correctly in both apps
```

**Step 3: Update ROADMAP.md**

Change Sprint 0.4 status from ⬜ to ✅ and update progress.

**Step 4: Commit documentation**

```bash
git add CHANGELOG.md ROADMAP.md
git commit -m "$(cat <<'EOF'
docs: mark Sprint 0.4 as complete

- Updated CHANGELOG with Sprint 0.4 deliverables
- Updated ROADMAP progress to 4/71 sprints

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
EOF
)"
```

---

## Summary

| Task | Description | Files |
|------|-------------|-------|
| 1 | Install Tailwind dependencies | package.json files |
| 2 | Create Tailwind config with design tokens | tailwind.config.ts, postcss.config.js, utils.ts |
| 3 | Configure apps to use Tailwind | postcss, tailwind configs, globals.css |
| 4 | Setup font loading | layout.tsx files |
| 5 | Create Button with CVA | button.tsx |
| 6 | Create Input component | input.tsx |
| 7 | Create Label component | label.tsx |
| 8 | Create Card component | card.tsx |
| 9 | Update web app | page.tsx |
| 10 | Update admin app | page.tsx, login/page.tsx |
| 11 | Final verification | CHANGELOG.md, ROADMAP.md |

**Total commits:** 11
**Gate verification:** `pnpm build && pnpm type-check && pnpm lint`
